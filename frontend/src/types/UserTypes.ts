export type User = {
  name: string;
  username: string;
  email: string;
  password: string;
  role: string;
  avatar: string;
  refreshToken: string;
  accessToken?: string;
  _id: string;
  createdAt: string;
  updatedAt: string;
};
